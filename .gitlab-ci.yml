# .gitlab-ci.yml

stages:
  - build

build:
  stage: build
  image: node:latest
  before_script:
    - npm install
  only:
    - main
  script:
    - docker build -t travis .
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push travis
  tags: 
    - docker