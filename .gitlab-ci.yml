# .gitlab-ci.yml

stages:
  - build

build:
  stage: build
  image: node:latest
  services:
    - docker:dind
  script:
    - docker build -t travis .
    - docker login gitlab.labranet.jamk.fi:4567
    - docker push travis
  tags: 
    - docker