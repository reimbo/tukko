# .gitlab-ci.yml

stages:
  - build

build:
  stage: build
  image: node:latest
  services:
    - docker:dind
  script:
    - docker build -t travis .
    - curl "https://gitlab.example.com/api/v4/projects?private_token=glpat-PTRMb9bmUZ2BuGDz8_9F"    
    - docker push travis
  tags: 
    - docker
